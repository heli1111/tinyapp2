<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <%- include partials/_header %>

  <!--display the shortURL and longURL, link shortURL to redirect to longURL-->
  <li><a href='/u/<%= shortURL %>/'><%= shortURL %></a><%= `:  ${longURL}` %>
  </li>

  <!-- submit an updated URL -->
    <form method="POST" action="/urls/<%= shortURL %>">
        <li>
        <input id="longURL" type="text" name="longURL" value="<%= longURL %>" style="width: 200px">
        <input type="submit" value="Edit">
        </li>
    </form>

</body>
</html>
